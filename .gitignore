# macOS
.DS_Store
.claude/.DS_Store

# Secrets — never commit real env files (the *.example templates ARE tracked)
.env
*.env
**/.env.local
**/.env.*.local

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/

# Database / process state
api/data/lastecho.db
api/data/lastecho.db-*

# Build artifacts / caches (regenerated; should not be in version control)
client.zip
client/.vite/
client/dist/
site/
node_modules/
