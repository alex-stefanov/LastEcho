fastapi>=0.110
uvicorn[standard]>=0.29

# outreach (the "response layer")
anthropic>=0.40        # outreach.py drafting (falls back to a template if unset)
reverse_geocoder>=1.5   # offline lat/lng -> country, no API key, no network call
httpx>=0.27             # ROR API lookups for the national tier
